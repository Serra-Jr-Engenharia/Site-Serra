export const Device = {
  Screen: {
    width: window.innerWidth,
    height: window.innerHeight,
  },

  FontSize: {
    extraSmall: window.innerHeight * 0.02,
    small: window.innerHeight * 0.021,
    standart: window.innerHeight * 0.022,
    medium: window.innerHeight * 0.023,
    large: window.innerHeight * 0.024,
    xLarge: window.innerHeight * 0.035,
  },

  Color: {
    lightBlue: '#04ADD7',
    darkBlue: '#001830',
    orange: '#FF7400'
  }
};
